# DEVİR NOTU — 2026-05-14

> Yeni Claude session bu dosyayı **ilk olarak** okumalı. Sonra:
> `customer_agent/CLAUDE.md`, `mind-agent/CLAUDE.md`,
> `mind-agent/docs/ROLLBACK.md`, ve bir önceki `docs/DEVIR-2026-05-01.md`.
>
> **🟢 SON DURUM:** Marka kimliği sistemi (Faz A→D) baştan sona
> kuruldu, test edildi, çalışıyor — **ama HENÜZ CANLIDA DEĞİL** (PR #10
> draft, Cloud Run hâlâ v1.21.2). Zernio webhook bug'ı **canlı n8n'de
> düzeltildi ve kanıtlandı**. Kalan iş listesi (A1.1→D3) Bölüm 6'da.
>
> **NEREDE KALDIK:** Bölüm 6'daki backlog'dan devam. Önerilen sıra:
> **C1 Vercel → C2 Faz B2 → A1 yeni workflow'lar.** Deploy + güvenlik +
> test temizliği (Bölüm D) EN SONA bırakıldı (kullanıcı kararı).

---

## 🔴 ROLLBACK — "Portalın şu anki haline her zaman dön" (kayıt: 2026-05-18)

> Bu kayıt, **2026-05-18 itibarıyla CANLI olan durumu** dondurur. Branch'ler
> `main`'e merge edilip deploy edildikten sonra bir şey bozulursa, portalı
> AYNEN bu hale döndürmek için aşağıyı uygula. Üretim her repoda `main`'den
> çalışıyor; feature branch'lerdeki işler **henüz canlıda DEĞİL**.

**Canlı referans noktaları (2026-05-18, dönülecek "şu anki hal"):**

| Repo | Canlı `main` SHA | Çalışma zamanı / platform |
|---|---|---|
| mind-id | `adc20b0` (2026-05-08) | Vercel — production deployment |
| mind-agent | `f583020` (2026-05-01) | Cloud Run revision `agents-sdk-api-00012-gln` (v1.21.2) |
| customer_agent | `90e60d8` (2026-05-04) | n8n Cloud (workflow'lar canlıda, git'ten bağımsız) |

### Geri dönüş komutları

**1) mind-id (Vercel) — en güvenli yol kod değil, platform:**
```
# Git: main yanlışlıkla ilerletildiyse canlı SHA'ya sabitle
cd mind-id && git checkout main && git reset --hard adc20b0
# (force push yalnızca kullanıcı onayıyla:)
# git push --force origin main
```
Tercih edilen: Vercel panosu → mind-id → Deployments → `adc20b0`
commit'inin deployment'ı → **"Promote to Production"** (anında geri alır,
git'e dokunmadan).

**2) mind-agent (Cloud Run) — git reset canlıyı geri ALMAZ, trafik revize edilir:**
```
cd mind-agent && git checkout main && git reset --hard f583020
# Canlı geri alma (trafiği eski revision'a ver):
gcloud run services update-traffic agents-sdk-api \
  --to-revisions agents-sdk-api-00012-gln=100 --region <bölge>
```

**3) customer_agent (n8n) — git merge n8n'i DEĞİŞTİRMEZ:**
n8n workflow'ları n8n Cloud'da yaşar; geri dönüş `mind-agent/docs/ROLLBACK.md`
+ `customer_agent/n8n/backups/` cold backup JSON'larından workflow restore
ile yapılır (hard-delete değil archive). Git tarafı:
```
cd customer_agent && git checkout main && git reset --hard 90e60d8
```

> Not: Hiçbir repoda commit edilmemiş/push edilmemiş değişiklik yok
> (2026-05-18). "Şu anki hal" = yukarıdaki üç `main` SHA'sı + listelenen
> Vercel/Cloud Run/n8n çalışma zamanı durumu.

---

## ✅ 2026-05-18 CANLI DEPLOY — MindBot lead sayımı düzeltildi

**Sorun:** MindBot "kaç sıcak lead var" → hep uydurma "2000"; Sales Analyst
32.6 sn'de hata.

**Kök nedenler (sırayla bulundu/çözüldü):**
1. Vercel `FIREBASE_*` servis hesabı anahtarı geçersizdi (`16 UNAUTHENTICATED`)
   + Netlify kalıntısı `NODE_OPTIONS=--openssl-legacy-provider`. → Vercel'de
   yeni servis hesabı anahtarı girildi (Production, 3 değişken) + Node 20 +
   Production deployment `7KTb1wQJG`/`da0dd1d` redeploy. "Error fetching
   settings" bitti (log: "Using PRODUCTION server URL").
2. Sales Analyst `gpt-4o` kullanıyordu; Zernio MCP ~80 tool şeması prompt'u
   ~30k token'a şişirip OpenAI org 30k TPM limitini aşıyordu (429 → ~30s →
   görev çöküyordu). → `sales_analyst_agent` modeli `gpt-4o-mini`'ye
   sabitlendi (commit `8e05221`).
3. `_count_leads_impl` `len(_fetch_all())` ile sayıyordu; `_fetch_all`
   sayfa ilerletmiyor (offset yok), `hard_cap=2000`'e şişiyor → ≥100
   sonuç hep "2000". → `NocoDBClient.count_records` (NocoDB v2
   `/records/count`) eklendi, `count_leads` ona bağlandı (commit `f512c77`).

**Doğrulama:** NocoDB gerçek `/records/count` → Sıcak=133, Toplam=239.
MindBot canlıda **133** döndü (eşleşti). Sales Analyst yeşil & hızlı.

**Branch:** `claude/add-hot-leads-count-LJNi7` (mind-agent) — canlı dal bu.
mind-id Vercel düzeltmesi Production env + Node 20 ayarı (kodsuz).

**🔴 CANLI ROLLBACK NOKTALARI (2026-05-18 sonu):**
| Kaynak | Şu anki (iyi) | Eski (geri dönüş) |
|---|---|---|
| Cloud Run revision | `agents-sdk-api-00036-44d` (LATEST, %100) | `agents-sdk-api-00034-vgb` |
| mind-agent git | `f512c77` | `38e24cc` (deploy öncesi) |

Acil Cloud Run geri dönüş:
```
gcloud run services update-traffic agents-sdk-api --region=us-central1 \
  --project instagram-post-bot-471518 --to-revisions=agents-sdk-api-00034-vgb=100
```
Git geri dönüş: `git revert f512c77 8e05221` → yeniden deploy.

**⚠️ Açık güvenlik işi:** `NOCODB_API_TOKEN` Cloud Run'da düz metin env'de
ve bu oturumda sohbette görüldü → **rotate edilmeli** (yeni token üret,
Cloud Run + mind-id Vercel env güncelle, eskisini iptal et).

---

## 1. Bu Oturumda Ne Yapıldı (2026-05-14)

### Marka kimliği sistemi — mind-agent PR #10 (DRAFT, branch `claude/greeting-implementation-Up3H3`)

| Faz | İçerik | Test |
|---|---|---|
| **A** | `BrandIdentity` Pydantic schema (6 alt-model) + Firestore tools (`fetch/update_brand_identity`) | 40 ✅ |
| **A2** | +10 yeni alan (Seyma 43-soru listesinden): keywords, agent_role, address_form(siz/sen), emoji_usage, hook_style, cta_templates, avoid_topics, gender, ses, motivations, required_hashtags, price_segment | 37 ✅ |
| **B1** | Brand Synthesis Agent (`src/agents/brand_synthesis_agent.py`) — ham veriden marka kartı önerir, source='ai_synthesis' | 8 ✅ |
| **C** | marketing/image/video agent'ları her görevde `fetch_brand_identity` çağırıp prompt'a enjekte ediyor | 6 ✅ |
| **C+ fix 1** | Image tool-level retry (`_generate_with_retry`) — transient Gemini hataları agent'ı pes ettiriyordu, artık otomatik retry | 9 ✅ |
| **C+ fix 2** | `prompt_summary` → `prompt_summary_image` + `prompt_summary_caption` ayrıldı (constraint overload çözümü) | 9 ✅ |
| **C+ fix 3** | image/video instruction: "REFINES değil REPLACES", "YAZI koyma, POSTER yapma, doğal sahne üret" | (üstteki) |
| **D** | `OpenAIImageClient` + factory toggle (`get_image_generation_client` model adına göre Gemini/OpenAI seçer) | 10 ✅ |

**Toplam ~125 test yeşil, sıfır regresyon.** Tüm fazlar pure additive.

### Canlı A/B test bulguları (Slowdays AI, `slowdays_ai_test` business)
- Caption: marka kimliksiz "turistik genel" → marka kimlikli "B2B, doğru ton, gerçek CTA, zorunlu #SlowdaysAI, tercih kelimeler" — **net fark, başarılı**
- Görsel: 3 iterasyon sonunda doğal sahne + marka renk uyumu — kabul edilebilir (mükemmel değil; B2.2/A3 ile iyileşebilir)
- Image model Firestore'da **`gpt-image-1`'e çekildi** (kullanıcı kararı; eskisi `gemini-2.5-flash-image`)

### customer_agent — Zernio webhook fix (CANLI UYGULANDI)
- **Sorun:** Lead Toplama Agent (`l31p16NRZeyk4eEm`) `Calculate Lead Score` node'u Zernio payload formatını (`message.sender.name/phoneNumber`) parse etmiyordu → WhatsApp/Instagram lead'leri NocoDB'ye BOŞ düşüyordu (gelir kaybı).
- **Çözüm:** code node Zernio message/comment + generic formatları detect ediyor + boş-kayıt koruması. Repo commit `0de44b9`.
- **Canlıya uygulandı:** n8n API ile PUT (kullanıcının 7-günlük token'ı, sonra silinmeli). Yedek: `/tmp/lead-toplama-BACKUP.json`.
- **Kanıtlandı:** Test webhook → NocoDB Leadler Id 196 "Test Otel Sahibi" / "+905551112233" / IG DM / skor 15 DOLU düştü (eskiden boştu). Test kaydı silinmeli (Id 196).

### Dokümantasyon düzeltmeleri
- `mind-agent/CLAUDE.md`: yanlış v1.20.0 → doğru **v1.21.2** + brand identity özeti
- `customer_agent/AGENT-MIMARISI-MASTER.md`: 32 yerde "Airtable" → "NocoDB"
- `mind-agent/docs/ROLLBACK.md`: oluşturuldu (canlı snapshot + 5 rollback senaryosu + n8n değişiklik kaydı Bölüm 4.5)

---

## 2. Canlı Sistemde Ne DEĞİŞTİ / DEĞİŞMEDİ

| Sistem | Durum |
|---|---|
| **Cloud Run mind-agent** | ⚪ DEĞİŞMEDİ — `agents-sdk-api-00012-gln` (v1.21.2). Brand identity CANLIDA DEĞİL. |
| **n8n Lead Toplama Agent** | 🔴 DEĞİŞTİ — Zernio fix canlıya uygulandı (Bölüm 1). Workflow ACTIVE kaldı. |
| **mind-id (Netlify)** | ⚪ DEĞİŞMEDİ — 2 Mayıs manuel publish, auto-build kapalı |
| **Firestore `settings/app_settings.imageGenerationModel`** | 🟡 DEĞİŞTİ — `gpt-image-1` (eskisi `gemini-2.5-flash-image`). Cloud Run v1.21.2 bu ayarı kullanır AMA eski kod gpt-image client'ı bilmez → fallback Gemini. Faz Z deploy sonrası gerçekten gpt-image-1 çalışır. |
| **Firestore test verisi** | 🟡 EKLENDİ — `businesses/slowdays_ai_test` + `brand_identity/v1` (manual). Gerçek mind-id listesini etkilemez. |

---

## 3. Kritik Sabitler (değişmedi — DEVIR-2026-05-01'den)

```
GCP_PROJECT          : instagram-post-bot-471518
CLOUD_RUN_SERVICE    : agents-sdk-api  (us-central1)
CLOUD_RUN_URL        : https://agents-sdk-api-704233028546.us-central1.run.app
CURRENT_REVISION     : agents-sdk-api-00012-gln  (v1.21.2)  ← ROLLBACK hedefi
NOCODB_BASE_URL      : http://34.26.138.196
NOCODB_LEADS_TABLE   : m5lcgc5ifeqh38h  (Leadler)
NOCODB_MSGS_TABLE    : mx3kbw2vhwimxjf  (Etkilesimler)
n8n                  : https://mindidai.app.n8n.cloud
  Lead Toplama Agent : l31p16NRZeyk4eEm  (ACTIVE, Zernio-fixed)
PR #10 (mind-agent)  : claude/greeting-implementation-Up3H3 (DRAFT)
customer_agent branch: claude/greeting-implementation-Up3H3
```

Cloud Shell env (her reconnect sonrası tekrar set — kalıcı değil):
```bash
gcloud config set project instagram-post-bot-471518
gcloud secrets versions access latest --secret=firebase-credentials > /tmp/firebase-active.json
export FIREBASE_CREDENTIALS_FILE=/tmp/firebase-active.json
export OPENAI_API_KEY=$(gcloud run services describe agents-sdk-api --region=us-central1 --format=json | python3 -c "import json,sys;d=json.load(sys.stdin);[print(e['value']) for e in d['spec']['template']['spec']['containers'][0].get('env',[]) if e['name']=='OPENAI_API_KEY']")
export GOOGLE_AI_API_KEY=$(gcloud run services describe agents-sdk-api --region=us-central1 --format=json | python3 -c "import json,sys;d=json.load(sys.stdin);[print(e['value']) for e in d['spec']['template']['spec']['containers'][0].get('env',[]) if e['name']=='GOOGLE_AI_API_KEY']")
```

---

## 4. Test/Yardımcı Script'ler (mind-agent/scripts/)

| Script | Ne yapar |
|---|---|
| `fill_brand_identity.py <bid>` | İnteraktif 43-soru marka kimliği doldur (source=manual) |
| `show_brand_identity.py <bid>` | brand_identity'i + prompt_summary'leri göster |
| `content_brand_ab.py <bid>` | Tam akış A/B (marketing→image, marka var/yok) |
| `image_brand_ab.py <bid> --debug` | İzole image A/B + retry + debug.log |
| `debug_image_brand.py <bid>` | Direkt Gemini'ye 5 prompt varyantı (tanı) |

---

## 5. Bekleyen KULLANICI Aksiyonları (acil değil ama temizlik)

1. NocoDB test kaydı sil: Leadler Id 196 (`curl DELETE ...records -d '[{"Id":196}]'`)
2. n8n `claude-zernio-fix` API key'ini sil (Settings → n8n API)
3. Cloud Shell: `unset N8N_TOKEN NOCO_TOKEN`
4. `/tmp/lead-toplama-BACKUP.json`'ı kalıcı güvenli yere kopyala (rollback için, /tmp geçici)

---

## 6. KALAN İŞ LİSTESİ — BURADAN DEVAM EDİLECEK

> Kullanıcı kararı: **Deploy + güvenlik + test temizliği EN SONA (Bölüm D).**
> Önerilen başlangıç sırası: **C1 → C2 → A1.**

### A) customer_agent
| # | İş | Efor |
|---|---|---|
| A1.1 | **LinkedIn Outreach workflow** (yok, master mimaride var — en yüksek getirili) | 1-2 gün |
| A1.2 | Clay Yerel Arama workflow — **KARAR (2026-05-15): Kullanıcı B'yi seçti = Clay planı yükseltilecek (webhook çıkışı için), sonra Clay→n8n→NocoDB tam otomatik kurulacak. Ücretsiz planda tüm webhook/HTTP-API çıkışları kilitli; "Send table data" sadece Clay-içi tabloya yazıyor. Plan yükseltilene kadar ERTELENDİ. NocoDB hazır: cred "NocoDB Token account 2", proje `ps9dj2fqrh823av`, lead tablosu `m5lcgc5ifeqh38h`. Mevcut Clay tablosu: "Find local businesses using Google Maps Table" (82 satır: isim/maps/website/telefon/adres/rating/reviews).** | 1 gün |
| A1.3 | IG DM Bot workflow (lead için; mevcut instagram_* content için) | 1 gün |
| A2.1 | Meta Lead Ads — App Review onayı sonrası smoke test | 1 saat |
| A2.2 | Facebook Lead Ads Performance Tracker aktive (`47pTxyFQPaQxEWxU`) | 2 saat |
| A2.3 | Çöp workflow sil — **TAMAMLANDI (2026-05-15): Her ikisi de (`zI7CyXdyHmxX9mF0`, `rNpzfRmdNLnOwXBR`) n8n'de ZATEN ARŞİVLİ. Aktif listede değiller, hard-delete gerekmedi. İşlem yok.** | 0 |
| A2.4 | Takip/İtiraz/Upsell Zernio uyumu kontrol — **YAPILDI (2026-05-15): Takip & Upsell webhook ALMIYOR (Schedule→NocoDB), risk YOK, temiz. İTİRAZ agent (`9nTdKNPLCjo8DKfE`) webhook alıyor ve lead-toplama ile AYNI sınıf bug'a sahipti: sadece generic `body.mesaj` bekliyor, Zernio message/comment mapping yok, boş-kayıt guard yok → Zernio'dan itiraz gelince LLM'e boş mesaj + NocoDB'ye çöp boş satır. Ek: `lead_adi`'ye e-posta yazıyordu. DÜZELTME repo cold-backup JSON'a uygulandı: Webhook→`Normalize Payload`(yeni code node, Zernio A/B/C + boş guard)→LLM(`{{$json.mesaj}}`)→Code(normalize'den okur, `lead_adi`=ad||email)→NocoDB(`lead_adi`={{$json.lead_adi}}). CANLI DURUM (2026-05-15): Yedek alındı (`n8n/backups/itiraz-agent.live-2026-05-15.pre-fix.json`). SDK ile update_workflow yapıldı → düzeltilmiş **taslak sürüm** kaydedildi (versionId 08f0ed0e) AMA `newCredential()` placeholder'ları mevcut kimliklere BAĞLANMADI (autoAssignedCredentials boş) ve sürüm YAYINLANMADI. `activeVersionId` hâlâ eski d7e4efcc → **üretim eski çalışan sürümde, REGRESYON YOK**. Düzeltmeyi canlıya almak için KULLANICI AKSİYONU gerekli: n8n UI'da Gemini/NocoDB/Gmail node'larına mevcut kimlikleri (Google Gemini(PaLM) Api account 2 / NocoDB Token account 2 / Gmail account) seç + Save/publish + test. Zernio `itiraz-gelen`'e bağlı değil (kullanıcı teyidi) → bug latent, aciliyet düşük.** | 1 saat |
| A2.5 | **YENİ BULGU (2026-05-15 Test B):** İtiraz düzeltmesi canlıda YyAYINLANDI + uçtan uca doğrulandı (Webhook→Normalize[Zernio mesaj doğru çözüldü]→Gemini[itiraz_turu=Fiyat]→Code[lead_adi=ad]→NocoDB[satır yazıldı] ✅). ANCAK **Gmail "Send a message" node'u "The resource owner or authorization server denied the request" hatası veriyor** — Gmail OAuth (cred `Gmail account` / id mVFOSbLiq27Ig3zg) reddediyor. Bizim değişiklikten BAĞIMSIZ, eski sürümde de patlardı → **itiraz/sıcak-lead bildirim mailleri Seyma'ya ULAŞMIYOR (muhtemelen bir süredir)**. KAPSAM DOĞRULANDI: TEK "Gmail account" cred'ini 7 workflow'un HEPSİ kullanıyor (itiraz, lead-toplama[sıcak lead uyarısı], meta-lead-ads[Seyma uyarısı], musteri-mail-otomasyonu[MÜŞTERİYE giden mail], referans, takip, upsell). Yani tüm satış e-posta katmanı (iç bildirim + dış müşteri maili) ŞU AN SESSİZ. Tek nokta arıza → tek çözüm: o Gmail cred'i 1 kez reconnect edilince 7 workflow birden düzelir. AKSİYON (yalnız kullanıcı yapabilir, OAuth): n8n → Gmail credential → "Sign in with Google". **ÇÖZÜLDÜ (2026-05-15): Kullanıcı Gmail OAuth iznini (tüm scope'lar) yeniden verdi. Doğrulama: İtiraz exec #254 uçtan uca SUCCESS — Send a message Gmail message id döndü (SENT/INBOX). 7 workflow'un tamamı için e-posta katmanı tekrar çalışıyor.** Test izleri: NocoDB itiraz tablosunda "TEST - Claude Deneme Musteri" + "TEST - Claude Gmail Dogrulama" satırları silinebilir. Test izi: NocoDB itiraz tablosunda "TEST - Claude Deneme Musteri" satırı silinebilir. | 30 dk |
| A3.1 | customer_agent/CLAUDE.md güncelle (Meta Lead Ads "pasif" → ACTIVE; n8n envanteri eski) | 30 dk |
| A3.2 | customer_agent PR #1 draft akıbeti netleştir | 15 dk |

### B) mind-agent
| # | İş | Efor |
|---|---|---|
| B1.1 | LinkedIn agent (`src/agents/sales/`) | 2-3 gün |
| B1.2 | Clay agent | 1-2 gün |
| B1.3 | İtiraz agent (n8n→SDK) | 1-2 gün |
| B1.4 | **Meta Agent genişletme** — Facebook Ads Manager API (CTR/CPC/CPL, A/B, günlük rapor). **NOT (2026-05-15): Meta resmi "Marketing API MCP Server" çıkardı (Claude Connectors ile 60sn kurulum; rapor/kampanya/katalog/diagnostics). Eforu ciddi düşürür, "resmi yol" = ban riski az. Sınır: sadece Meta, hesap başına sınırlı rollout (20'de 1), bazı işlevler eksik, 24 saatlik yeni özellik. LinkedIn'e (A1.1) FAYDASI YOK — LinkedIn resmi MCP'si yok, ToS sorunu sürüyor. Bu çalışma ortamında zaten bir Meta Ads MCP bağlı. TEST EDİLDİ (2026-05-15): 5 hesap görünüyor (Slowdays AI dahil) ama HEPSİNDE `is_ads_mcp_enabled=false` ("rollout kademeli, sonra dene") → şu an hiçbir işlem yapılamıyor, kazanım 0. Meta açana kadar beklemede. İLERİDE periyodik kontrol et; açılınca Meta Agent kurulumunda bu MCP yolu değerlendirilecek.** | 1 hafta |
| B2.1 | Faz A3 — per-platform format kuralları (IG/LinkedIn/TikTok ayrı) | 3-4 saat |
| B2.2 | Brand Synthesis zenginleştirme — web sitesi + Instagram geçmiş post analizi | 1 gün |
| B3.1 | Video SDK geçişi (eski google-genai → yeni, ASKIDA) | 1 gün |
| B3.2 | `create_lead` deprecated kaldır (duplicate riski) | 1 saat |
| B3.3 | Test boşlukları (video E2E, orchestrator routing, marketing/analysis agent testleri) | 1-2 gün |

> **C2/B DURUM (2026-05-15):** Kullanıcı B'yi seçti (C2 + panelde Marka Kimliği editörü, ayrı sidebar sayfası, Faz1→onay→Faz2). **Faz 1 + Faz 2 BİTTİ ve push edildi** (mind-id branch `claude/vercel-migration-aKUyl`). **Faz 1** commit `c20b9ac` (öncesi `da0dd1d`): `lib/brandIdentity.ts` + form onayında `brand_identity/v1` otomatik yazımı + client helper. **Faz 2** commit `b441086` (öncesi `c20b9ac`): sol menüde "Marka Kimliği" sayfası + işletme seçici + 6 bölümlü editör (`components/businesses/brand-identity-editor.tsx`), Kaydet→`brand_identity/v1`. **ROLLBACK:** Faz 2 için `git revert b441086`, Faz 1 için `git revert c20b9ac`. tsc: yeni dosyalarda 0 hata (page.tsx'teki 3 hata ÖNCEDEN vardı, MobileSidebar tip uyumsuzluğu, kapsam dışı). **Kalan:** eski işletmelere defter doldurma (editörden tek tek ya da toplu), opsiyonel Brand Synthesis AI.

### C) mind-id  ← **ÖNERİLEN BAŞLANGIÇ**
| # | İş | Efor |
|---|---|---|
| ~~C1~~ | ✅ **TAMAM (2026-05-15)** — Vercel migrasyonu bitti, `mind-id.vercel.app` canlı çalışıyor (kullanıcı teyidi). Kod: `claude/vercel-migration-aKUyl`. Netlify yedek duruyor (Faz 6 ~1 hafta sonra). | — |
| **C2** | **Faz B2 — form ↔ BrandIdentity bağlama** — 9 form section'ı yeni 43-alan şemasına bağla (panelden doldur, Cloud Shell'e gerek kalmasın) ← **SIRADAKİ** | 3-4 saat |
| C3 | `/satis` sayfası — NocoDB Leadler read-only dashboard | 2-3 saat |
| C4 | VillageCanvas aktivasyonu (Vercel sonrası, tek satır swap) | 10 dk |
| C5 | `Microsoft.Management.Deployment.winmd` sil (yanlış commit'lenmiş binary) | 5 dk |
| C6 | Branch protection (main: PR + 1 approval) | 30 dk |

### D) EN SONA (kullanıcı kararı)
| # | İş |
|---|---|
| D1 | 🔴 **Güvenlik temizliği** — key rotate (OpenAI/Google AI/Late/Zernio), repo'dan `serviceAccount.json`+`gcp-service-account.json` sil, NocoDB `claude-setup`/n8n eski token revoke, `.gitignore` |
| D2 | 🟡 **Faz Z deploy** — PR'lar merge + Cloud Run v1.22.0 (brand identity + Zernio + retry + gpt-image-1 canlıya) |
| D3 | 🧹 **Test verisi temizliği** — `slowdays_ai_test` business+brand_identity, NocoDB test kayıtları, `ab_outputs/`, /tmp yedekler, n8n geçici key |

---

## 7. İlk Komut (Yeni Session İçin)

```bash
# 1. Bu notu + ROLLBACK.md + iki CLAUDE.md oku
# 2. Branch doğru mu?
cd ~/mind-agent && git checkout claude/greeting-implementation-Up3H3 && git pull
cd ~/customer_agent && git checkout claude/greeting-implementation-Up3H3 && git pull
# 3. Kullanıcıya sor: "C1 Vercel migrasyonundan mı başlayalım, yoksa
#    başka bir önceliğin mi var?" (Bölüm 6 backlog'u göster)
# 4. Deploy/güvenlik/test-temizliği SON — kullanıcı böyle istedi.
```

---

## 8. Kullanıcı Tercihleri (DEVIR-2026-05-01'den + bu oturum)

- Türkçe yanıt, sade dil (Seyma kod bilmiyor, iş penceresinden bakıyor)
- Komutları tek tek ver, çıktı bekle
- **ROLLBACK ZORUNLU (kalıcı kural, 2026-05-15):** Canlı/geri-dönülmesi zor HER değişiklikten ÖNCE mevcut hali repoya yedekle (n8n → `n8n/backups/<ad>.<tarih>.pre-<değişiklik>.json`; kod → commit), geri dönüş yolunu bu nota yaz. Silme yerine geri-dönülebilir yöntem (archive). Kural 3 repo CLAUDE.md'sine de işlendi. "Her zaman bu haline geri dönebilelim" şarttır.
- Canlı sisteme dokunmadan önce yedek al + onay iste
- Test-first geliştirme (mind-agent/CLAUDE.md kuralı)
- A/B karşılaştırmaları kullanıcıya somut fark gösterir (caption/görsel)
- Deploy + güvenlik + test temizliği bilinçli olarak en sona bırakıldı
